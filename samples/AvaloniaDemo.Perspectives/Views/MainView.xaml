<UserControl x:Class="AvaloniaDemo.Views.MainView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:id="clr-namespace:Dock.Avalonia;assembly=Dock.Avalonia"
             xmlns:idc="clr-namespace:Dock.Avalonia.Controls;assembly=Dock.Avalonia"
             xmlns:views="clr-namespace:AvaloniaDemo.Views;assembly=AvaloniaDemo"
             mc:Ignorable="d"
             d:DesignWidth="1280" d:DesignHeight="680"
             id:DockProperties.IsDragEnabled="True" id:DockProperties.IsDropEnabled="True">
    <UserControl.Styles>
        <StyleInclude Source="avares://AvaloniaDemo/Layouts/Default.xaml"/>
    </UserControl.Styles>
    <Grid RowDefinitions="Auto,*,25" Background="Transparent">
        <views:MenuView Grid.Row="0"/>
        <idc:DockControl x:Name="dockControl" Classes="Default" Margin="4" Grid.Row="1"/>
        <TextBlock Text="{Binding #dockControl.Layout.FocusedDockable}" Margin="4" Grid.Row="2" />
    </Grid>
</UserControl>
