<UserControl x:Class="DockReactiveUICanonicalSample.Views.Pages.ProjectFilePageView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DockReactiveUICanonicalSample.ViewModels.Pages"
             x:CompileBindings="True"
             x:DataType="vm:ProjectFilePageViewModel">
  <Grid RowDefinitions="Auto,*" RowSpacing="8" Margin="8">
    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
      <Button Content="Back"
              Command="{Binding GoBack}"
              VerticalAlignment="Center" />
      <StackPanel Grid.Column="1" Spacing="2">
        <TextBlock Text="{Binding File.Name}" FontSize="18" FontWeight="SemiBold" />
        <TextBlock Text="{Binding Project.Name}" FontSize="12" Opacity="0.7" />
      </StackPanel>
    </Grid>
    <DockControl Grid.Row="1" Layout="{Binding WorkspaceLayout}" />
  </Grid>
</UserControl>
